## Packages
recharts | Data visualization for skill gap charts (radar charts)
framer-motion | Smooth page transitions and UI animations
date-fns | Formatting dates for analysis history

## Notes
- Tailwind config needs 'Plus Jakarta Sans' font family added
- Auth flows use /api/register and /api/login with session cookies
- Analysis results stored as JSONB, frontend needs to parse structure match SkillGapResult type
